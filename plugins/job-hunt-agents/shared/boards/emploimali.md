# Board measurement — emploimali.net (Mali): the name has no delegation, on two public resolvers

<!-- verified: 2026-09-11 -->

<!-- hosts: emploimali.net, www.emploimali.net -->
<!-- script: none -->
<!-- countries: ML -->
<!-- witness: none — no HTTP request reached a host: the name does not resolve; the only measurement is DNS, on two public resolvers · 2026-09-11 -->

**This card is a measurement and not an adapter, and the measurement never
reached HTTP.** *`emploimali.net` was named in the Atlas's «twenty hosts named
and never asked» (#147) and in a survey's prose; it had no card.* **Asked,
there is nobody to ask.**

## Step 1 — the name does not resolve (2026-09-11 13:54–13:59 UTC)

```
local resolver     emploimali.net / www.emploimali.net    "nodename nor servname provided, or not known"   ×4
                   (bin/fetch-body.py → INDETERMINATE: robots.txt could not be read after 3 attempts)

1.1.1.1            emploimali.net   NXDOMAIN        www.emploimali.net   NXDOMAIN
8.8.8.8            emploimali.net   NXDOMAIN        www.emploimali.net   NXDOMAIN
```

**A DNS negative is checked by a SECOND RESOLVER, never by a second host, and
`SERVFAIL` is not `NXDOMAIN`.** Two independent public resolvers, both forms:
**the name has no delegation on 2026-09-11.** *An absence, dated — the same
class as `bestjobs.mg` the same afternoon — not a refusal, not a challenge,
not an indeterminate transport.*

## What this card does not say

Nothing about a board — there is no host to describe — and nothing about
Mali's market: `jobmali.md` is the other named host, and the country's one
adapter is elsewhere. **«`emploimali.net` is dead» is what this card can
write, with its date and its two resolvers.**
