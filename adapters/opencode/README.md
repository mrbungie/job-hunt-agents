# OpenCode adapter

Copy `skills` into the target project's `.opencode/skills` directory, then
merge this adapter's `opencode.json` into the target project's configuration.
No JavaScript plugin or lifecycle hooks are required.

For browser work, use `mcp-chrome` in the Chrome profile selected by
the person. `shared/browser-handoff.md` defines the required human handoffs.
