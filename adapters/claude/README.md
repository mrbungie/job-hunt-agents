# Claude Code adapter

The Claude distribution retains `.claude-plugin/plugin.json` and the upstream
skill layout. No lifecycle hook is installed. Run `bin/preflight.py` from the
bundle before a workflow when workspace or renderer diagnostics are useful.
